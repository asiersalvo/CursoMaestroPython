#Para poder recibir argumentos tenemos que usar:
import sys #libreria

print("Hola, bienvenido a tu primer script.")
#Esto solamente  nos muestra este mensaje por pantalla, pero no podemos recibir solo con esto argumentos
print(sys.argv)  #Con esto podremos ver los argumentos que estamos recibiendo, el archivo ejecutado en si y luego el resto que vayamos metiendo
#Entonces lo que haremos ahora será coger esos argumentos y usarlos en nuestro programa como queramos
